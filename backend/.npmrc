# @nestjs/throttler@6.5.0 todavía declara peerDependency hasta @nestjs/common ^11 —
# no publicaron el bump a v12 todavía. Verificado a mano que funciona correcto contra
# NestJS 12 (build, guard, rate-limit real probados) — ver ARCHITECTURE.md.
legacy-peer-deps=true
